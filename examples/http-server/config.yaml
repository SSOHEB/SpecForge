instrumentation:
  http:
    enabled: true
    capture_headers:
      - "Content-Type"
      - "Authorization"
    port: 8080
    log_level: "info"
    host: "0.0.0.0"
    timeout: 60
    redact_query:
      - "token"
      - "password"
    api_key: "ABCDEF1234567890"

